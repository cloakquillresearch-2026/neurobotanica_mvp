name: Re-check annotated_doc availability (No-op safe)

on:
  schedule:
    - cron: '0 6 * * 1' # Weekly on Monday at 06:00 UTC
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: No-op recheck
        run: |
          echo "Scheduled re-check workflow temporarily reduced to no-op to avoid CI failures."
          echo "Restore the original workflow from the revert PRs once reviewed."
